fixtures:
  repositories:
    # apache:
    #   repo: 'git://github.com/puppetlabs/puppetlabs-apache.git'
    #   ref:  '1.0.1'
  forge_modules:
    stdlib:
      repo: 'puppetlabs-stdlib'
      ref:  '4.13.0'
    concat:
      repo: 'puppetlabs-concat'
      ref:  '2.2.0'
  symlinks:
    kerberos: "#{source_dir}"
