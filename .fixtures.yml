fixtures:
  repositories:
    # apache:
    #   repo: 'git://github.com/puppetlabs/puppetlabs-apache.git'
    #   ref:  '1.0.1'
  forge_modules:
    stdlib:
      repo: 'puppetlabs-stdlib'
      ref:  '4.16.0'
    concat:
      repo: 'puppetlabs-concat'
      ref:  '4.1.0'
  symlinks:
    kerberos: "#{source_dir}"
